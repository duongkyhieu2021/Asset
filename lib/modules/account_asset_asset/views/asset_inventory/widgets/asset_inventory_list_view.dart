import 'package:flutter/widgets.dart';

class AssetInventoryListView extends StatelessWidget {
  const AssetInventoryListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("AssetInventoryListView"),
    );
  }
}
